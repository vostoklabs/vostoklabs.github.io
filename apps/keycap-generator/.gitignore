node_modules/
dist/
*.log
.DS_Store

# Source CAD — not tracked; the converted public/keycaps/*.json are the committed artifact.
# Re-add your .stp/.step files here and run `npm run convert` to regenerate them.
Step files of keycaps/

# Full-res originals for the README shots; web-optimized copies live in docs/.
Media/

# MakerWorld SDK — proprietary/NDA material, never commit.
Makerworlf SDK example/
Makerworld SDK example/
**/makerworld-sdk/
__MACOSX/

# MakerWorld build layer: the SDK runtime + host simulator are NDA (see makerlab/README.md
# to reconstruct); the submission zip is a build artifact. The glue, config, and pack script
# ARE committed.
src/makerlab/lib/
makerlab/server.cjs
makerlab/*.zip
