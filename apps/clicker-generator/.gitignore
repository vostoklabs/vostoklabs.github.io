node_modules/
dist/
*.local
.DS_Store
.vite/
*.log

# local backups / archives (e.g. project zip snapshots)
*.zip

# local planning docs (AI agent manuals, not for the public repo)
New featrures dev plan.md
